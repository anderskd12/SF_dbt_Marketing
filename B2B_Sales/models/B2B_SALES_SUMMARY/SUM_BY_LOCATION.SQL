{{ config(materialized='table') }}
WITH SALES_BY_LOCATION AS (
SELECT OFFICE_LOCATION, SUM(TOTAL_AMOUNT) AS TOTAL_SALES FROM TICKET_SALES_RAW
GROUP BY OFFICE_LOCATION)
SELECT *, CURRENT_TIMESTAMP() AS TIME_LOADED FROM SALES_BY_LOCATION
ORDER BY 2 DESC
